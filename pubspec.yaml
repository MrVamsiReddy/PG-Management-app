name: pg_management
description: A complete role-based PG management platform.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  image_picker: ^1.2.3
  intl: ^0.19.0
  pdf: ^3.13.0
  printing: ^5.15.0
  share_plus: ^13.2.0
  supabase_flutter: ^2.15.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
